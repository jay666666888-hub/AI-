# AI第二大脑 V1 前端实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 实现完整的微信小程序前端，包含4 Tab导航、首页、任务中心、项目管理、习惯打卡、Inbox、记忆库、通知中心

**架构：** 微信原生开发，单文件页面（js/wxml/wxss/json），使用 `getApp()` 全局状态管理，Emoji图标

**技术栈：** 微信小程序原生开发

---

## 文件结构

```
frontend/
├── app.json           # TabBar配置 + 页面注册
├── app.js # 全局状态管理 + API方法
├── app.wxss           # 全局样式
├── pages/
│   ├── index/ # 首页（今日概览）
│   │   ├── index.js
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.json
│   ├── tasks/         # 任务中心
│   │   ├── index.js
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.json
│   ├── projects/      # 项目列表
│   │   ├── index.js
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.json
│   ├── mine/          # 我的（设置）
│   │   ├── index.js
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.json
│   ├── habits/        # 习惯打卡
│   ├── inbox/         # Inbox
│   ├── memory/        # 记忆库
│   └── notifications/ # 通知中心
├── components/         # 复用组件
│   ├── task-item/
│   ├── project-card/
│   └── stat-card/
└── utils/
    └── api.js         # API封装
```

---

## 任务清单

### 任务 1：配置 TabBar 和全局样式

**文件：**
- 修改：`frontend/app.json` - TabBar配置
- 修改：`frontend/app.wxss` - 全局样式变量

- [ ] **步骤 1：修改 app.json 配置 TabBar**

```json
{
  "pages": [
    "pages/index/index",
    "pages/tasks/index",
    "pages/projects/index",
    "pages/mine/index",
    "pages/habits/index",
    "pages/inbox/index",
    "pages/memory/index",
    "pages/notifications/index"
  ],
  "tabBar": {
    "color": "#999999",
    "selectedColor": "#667eea",
    "backgroundColor": "#ffffff",
    "borderStyle": "white",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页",
        "iconPath": "images/home.png",
        "selectedIconPath": "images/home-active.png"
      },
      {
        "pagePath": "pages/tasks/index",
        "text": "任务",
        "iconPath": "images/tasks.png",
        "selectedIconPath": "images/tasks-active.png"
      },
      {
        "pagePath": "pages/projects/index",
        "text": "项目",
        "iconPath": "images/projects.png",
        "selectedIconPath": "images/projects-active.png"
      },
      {
        "pagePath": "pages/mine/index",
        "text": "我的",
        "iconPath": "images/mine.png",
        "selectedIconPath": "images/mine-active.png"
      }
    ]
  }
}
```

- [ ] **步骤 2：创建全局样式 app.wxss**

```css
/* 全局样式变量 */
page {
  --color-primary: #667eea;
  --color-primary-end: #764ba2;
  --color-text: #1a1a1a;
  --color-text-secondary: #333333;
  --color-text-hint: #666666;
  --color-text-weaker: #999999;
  --color-bg: #f5f5f5;
  --color-card: #ffffff;
  --color-border: #f0f0f0;
  --color-success: #52c41a;
  --color-warning: #faad14;
  --color-error: #ff4d4f;
  
  --font-size-xl: 48rpx;
  --font-size-lg: 36rpx;
  --font-size-md: 28rpx;
  --font-size-sm: 24rpx;
  --font-size-xs: 22rpx;
  
  --spacing-xs: 8rpx;
  --spacing-sm: 16rpx;
  --spacing-md: 24rpx;
  --spacing-lg: 32rpx;
  --spacing-xl: 48rpx;
  
  --radius-sm: 8rpx;
  --radius-md: 16rpx;
  --radius-lg: 24rpx;
}

/* 全局样式 */
.container {
  padding: var(--spacing-md);
  background: var(--color-bg);
  min-height: 100vh;
}

.card {
  background: var(--color-card);
  border-radius: var(--radius-md);
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-md);
}
```

- [ ] **步骤 3：Commit**
```bash
cd /mnt/e/黑曜石/04_工作/AI第二大脑微信小程序/frontend
git add app.json app.wxss
git commit -m "feat: 配置TabBar和全局样式"
```

---

### 任务 2：实现首页（今日概览）

**文件：**
- 创建：`frontend/pages/index/index.js`
- 创建：`frontend/pages/index/index.wxml`
- 创建：`frontend/pages/index/index.wxss`
- 创建：`frontend/pages/index/index.json`

- [ ] **步骤 1：创建 index.json**

```json
{
  "navigationBarTitleText": "第二大脑",
  "enablePullDownRefresh": true
}
```

- [ ] **步骤 2：创建 index.js（逻辑）**

```javascript
const app = getApp()

Page({
  data: {
    inboxCount: 0,
    todayTasks: [],
    todayHabits: [],
    activeProjects: []
  },

  onLoad() {
    if (!app.globalData.token) {
      this.doLogin()
    }
  },

  onShow() {
    this.loadData()
  },

  onPullDownRefresh() {
    this.loadData().then(() => {
      wx.stopPullDownRefresh()
    })
  },

  async doLogin() {
    try {
      await app.login()
      this.loadData()
    } catch (e) {
      console.error('登录失败', e)
    }
  },

  async loadData() {
    try {
      // 获取 Inbox 数量
      const inboxRes = await app.request('/notes/inbox/count')
      this.setData({ inboxCount: inboxRes.count || 0 })

      // 获取活跃任务
      const tasksRes = await app.request('/tasks?status=active')
      this.setData({ todayTasks: tasksRes || [] })

      // 获取项目
      const projectsRes = await app.request('/projects')
      this.setData({ activeProjects: projectsRes || [] })

      // 获取今日习惯
      try {
        const habitsRes = await app.request('/tasks/habits/today')
        this.setData({ todayHabits: habitsRes.habits || [] })
      } catch (e) {
        console.log('习惯接口暂无数据')
      }
    } catch (e) {
      console.error('加载数据失败', e)
    }
  },

  async completeTask(e) {
    const { id } = e.currentTarget.dataset
    try {
      await app.request(`/tasks/${id}/complete`, { method: 'POST' })
      wx.showToast({ title: '已完成', icon: 'success' })
      this.loadData()
    } catch (e) {
      wx.showToast({ title: '操作失败', icon: 'none' })
    }
  },

  goToInbox() {
    wx.navigateTo({ url: '/pages/inbox/index' })
  },

  goToTasks() {
    wx.switchTab({ url: '/pages/tasks/index' })
  },

  goToProjects() {
    wx.switchTab({ url: '/pages/projects/index' })
  },

  goToHabits() {
    wx.navigateTo({ url: '/pages/habits/index' })
  }
})
```

- [ ] **步骤 3：创建 index.wxml（结构）**

```xml
<view class="container">
  <!-- 今日概览 -->
  <view class="overview">
    <view class="stat-card" bindtap="goToInbox">
      <text class="stat-num">{{inboxCount}}</text>
      <text class="stat-label">Inbox</text>
    </view>
    <view class="stat-card" bindtap="goToTasks">
      <text class="stat-num">{{todayTasks.length}}</text>
      <text class="stat-label">任务</text>
    </view>
    <view class="stat-card" bindtap="goToHabits">
      <text class="stat-num">{{todayHabits.length}}</text>
      <text class="stat-label">习惯</text>
    </view>
  </view>

  <!-- 今日习惯 -->
  <view class="section">
    <view class="section-header">
      <text class="section-title">🔥 今日习惯</text>
      <text class="more" bindtap="goToHabits">查看全部 ›</text>
    </view>
    <view class="habit-list" wx:if="{{todayHabits.length}}">
      <view class="habit-item" wx:for="{{todayHabits}}" wx:key="id">
        <view class="habit-info">
          <text class="habit-title">{{item.title}}</text>
          <text class="habit-time">{{item.scheduled_time || '--:--'}}</text>
        </view>
        <view class="habit-status {{item.is_completed_today ? 'completed' : ''}}">
          {{item.is_completed_today ? '✓' : '○'}}
        </view>
      </view>
    </view>
    <view class="empty" wx:else>暂无习惯</view>
  </view>

  <!-- 今日任务 -->
  <view class="section">
    <view class="section-header">
      <text class="section-title">📋 今日任务</text>
      <text class="more" bindtap="goToTasks">查看全部 ›</text>
    </view>
    <view class="task-list" wx:if="{{todayTasks.length}}">
      <view class="task-item" wx:for="{{todayTasks}}" wx:key="id">
        <view class="task-info">
          <text class="task-title">{{item.title}}</text>
          <text class="task-type">{{item.type === 'habit' ? '习惯' : item.type}}</text>
        </view>
        <view class="task-action" bindtap="completeTask" data-id="{{item.id}}">✓</view>
      </view>
    </view>
    <view class="empty" wx:else>暂无任务</view>
  </view>

  <!-- 进行中项目 -->
  <view class="section">
    <view class="section-header">
      <text class="section-title">📁 进行中项目</text>
      <text class="more" bindtap="goToProjects">查看全部 ›</text>
    </view>
    <view class="project-list" wx:if="{{activeProjects.length}}">
      <view class="project-item" wx:for="{{activeProjects}}" wx:key="id">
        <text class="project-title">{{item.title}}</text>
        <text class="project-progress">{{item.progress || 0}}%</text>
      </view>
    </view>
    <view class="empty" wx:else>暂无项目</view>
  </view>
</view>
```

- [ ] **步骤 4：创建 index.wxss（样式）**

```css
/* 概览卡片 */
.overview {
  display: flex;
  justify-content: space-between;
  margin-bottom: var(--spacing-md);
}

.stat-card {
  flex: 1;
  background: var(--color-card);
  border-radius: var(--radius-md);
  padding: var(--spacing-md);
  text-align: center;
  margin: 0 var(--spacing-xs);
}

.stat-num {
  display: block;
  font-size: var(--font-size-xl);
  font-weight: 700;
  color: var(--color-primary);
}

.stat-label {
  display: block;
  font-size: var(--font-size-sm);
  color: var(--color-text-hint);
  margin-top: var(--spacing-xs);
}

/* 区块 */
.section {
  background: var(--color-card);
  border-radius: var(--radius-md);
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-md);
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-sm);
}

.section-title {
  font-size: var(--font-size-md);
  font-weight: 600;
  color: var(--color-text);
}

.more {
  font-size: var(--font-size-sm);
  color: var(--color-primary);
}

/* 习惯项 */
.habit-item, .task-item, .project-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-sm) 0;
  border-bottom: 1rpx solid var(--color-border);
}

.habit-item:last-child, .task-item:last-child, .project-item:last-child {
  border-bottom: none;
}

.habit-info, .task-info, .project-item {
  flex: 1;
}

.habit-title, .task-title, .project-title {
  display: block;
  font-size: var(--font-size-md);
  color: var(--color-text-secondary);
}

.habit-time, .task-type, .project-progress {
  display: block;
  font-size: var(--font-size-xs);
  color: var(--color-text-weaker);
  margin-top: 4rpx;
}

.habit-status {
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  border-radius: 50%;
  background: var(--color-bg);
  color: var(--color-text-weaker);
  font-size: var(--font-size-md);
}

.habit-status.completed {
  background: var(--color-success);
  color: white;
}

.task-action {
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  border-radius: 50%;
  background: var(--color-primary);
  color: white;
  font-size: var(--font-size-md);
}

.empty {
  text-align: center;
  color: var(--color-text-weaker);
  padding: var(--spacing-lg) 0;
  font-size: var(--font-size-sm);
}
```

- [ ] **步骤 5：Commit**
```bash
git add pages/index/
git commit -m "feat: 实现首页（今日概览）"
```

---

### 任务 3：实现任务中心

**文件：**
- 创建：`frontend/pages/tasks/index.js`
- 创建：`frontend/pages/tasks/index.wxml`
- 创建：`frontend/pages/tasks/index.wxss`
- 创建：`frontend/pages/tasks/index.json`

- [ ] **步骤 1：创建 index.json**

```json
{
  "navigationBarTitleText": "任务中心",
  "enablePullDownRefresh": true
}
```

- [ ] **步骤 2：创建 index.js**

```javascript
const app = getApp()

const TASK_TYPES = ['全部', 'Todo', 'Habit', 'Schedule', 'Waiting']

Page({
  data: {
    currentType: '全部',
    taskTypes: TASK_TYPES,
    tasks: [],
    filterVisible: false,
    projects: []
  },

  onLoad() {
    this.loadTasks()
    this.loadProjects()
  },

  onShow() {
    this.loadTasks()
  },

  onPullDownRefresh() {
    this.loadTasks().then(() => wx.stopPullDownRefresh())
  },

  async loadTasks() {
    try {
      const res = await app.request('/tasks?status=active')
      this.setData({ tasks: res || [] })
    } catch (e) {
      console.error('加载任务失败', e)
    }
  },

  async loadProjects() {
    try {
      const res = await app.request('/projects')
      this.setData({ projects: res || [] })
    } catch (e) {
      console.error('加载项目失败', e)
    }
  },

  switchType(e) {
    const type = e.currentTarget.dataset.type
    this.setData({ currentType: type })
    this.loadTasks()
  },

  async completeTask(e) {
    const { id } = e.currentTarget.dataset
    try {
      await app.request(`/tasks/${id}/complete`, { method: 'POST' })
      wx.showToast({ title: '已完成', icon: 'success' })
      this.loadTasks()
    } catch (e) {
      wx.showToast({ title: '操作失败', icon: 'none' })
    }
  },

  goToCreate() {
    wx.navigateTo({ url: '/pages/tasks/create' })
  }
})
```

- [ ] **步骤 3：创建 index.wxml**

```xml
<view class="container">
  <!-- 类型切换 -->
  <scroll-view scroll-x class="type-tabs">
    <view class="tab {{currentType === item ? 'active' : ''}}" 
          wx:for="{{taskTypes}}" 
          wx:key="item"
          bindtap="switchType" 
          data-type="{{item}}">
      {{item}}
    </view>
  </scroll-view>

  <!-- 任务列表 -->
  <view class="task-list">
    <view class="task-item {{item.status === 'completed' ? 'completed' : ''}}" 
          wx:for="{{tasks}}" 
          wx:key="id">
      <view class="task-info">
        <text class="task-title">{{item.title}}</text>
        <view class="task-meta">
          <text class="task-type">{{item.type}}</text>
          <text wx:if="{{item.project_id}}" class="task-project">📁</text>
        </view>
      </view>
      <view class="task-action" bindtap="completeTask" data-id="{{item.id}}">
        {{item.status === 'completed' ? '✓' : '○'}}
      </view>
    </view>
    
    <view class="empty" wx:if="{{!tasks.length}}">
      暂无任务
    </view>
  </view>

  <!-- 底部添加按钮 -->
  <view class="fab" bindtap="goToCreate">+</view>
</view>
```

- [ ] **步骤 4：创建 index.wxss**

```css
/* 类型切换 */
.type-tabs {
  white-space: nowrap;
  background: var(--color-card);
  padding: var(--spacing-sm) 0;
  margin-bottom: var(--spacing-md);
}

.tab {
  display: inline-block;
  padding: var(--spacing-sm) var(--spacing-md);
  font-size: var(--font-size-sm);
  color: var(--color-text-hint);
  border-radius: var(--radius-lg);
  margin: 0 var(--spacing-xs);
}

.tab.active {
  background: var(--color-primary);
  color: white;
}

/* 任务列表 */
.task-list {
  background: var(--color-card);
  border-radius: var(--radius-md);
  padding: var(--spacing-md);
}

.task-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-sm) 0;
  border-bottom: 1rpx solid var(--color-border);
}

.task-item.completed .task-title {
  text-decoration: line-through;
  color: var(--color-text-weaker);
}

.task-info { flex: 1; }

.task-title {
  display: block;
  font-size: var(--font-size-md);
  color: var(--color-text-secondary);
}

.task-meta {
  display: flex;
  gap: var(--spacing-sm);
  margin-top: 4rpx;
}

.task-type, .task-project {
  font-size: var(--font-size-xs);
  color: var(--color-text-weaker);
}

.task-action {
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  border-radius: 50%;
  background: var(--color-bg);
  color: var(--color-text-weaker);
  font-size: var(--font-size-md);
}

.task-item.completed .task-action {
  background: var(--color-success);
  color: white;
}

/* 底部按钮 */
.fab {
  position: fixed;
  right: var(--spacing-lg);
  bottom: var(--spacing-xl);
  width: 100rpx;
  height: 100rpx;
  line-height: 100rpx;
  text-align: center;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-end));
  color: white;
  font-size: 60rpx;
  box-shadow: 0 4rpx 20rpx rgba(102, 126, 234, 0.4);
}

.empty {
  text-align: center;
  color: var(--color-text-weaker);
  padding: var(--spacing-xl) 0;
}
```

- [ ] **步骤 5：Commit**
```bash
git add pages/tasks/
git commit -m "feat: 实现任务中心"
```

---

### 任务 4：实现项目列表页

**文件：**
- 创建：`frontend/pages/projects/index.js`
- 创建：`frontend/pages/projects/index.wxml`
- 创建：`frontend/pages/projects/index.wxss`
- 创建：`frontend/pages/projects/index.json`

- [ ] **步骤 1-4：创建页面文件**（类似任务2/3的结构）

```javascript
// index.js
const app = getApp()
Page({
  data: {
    projects: []
  },
  onLoad() {
    this.loadProjects()
  },
  async loadProjects() {
    try {
      const res = await app.request('/projects')
      this.setData({ projects: res || [] })
    } catch (e) {
      console.error('加载项目失败', e)
    }
  },
  goToCreate() {
    wx.navigateTo({ url: '/pages/projects/create' })
  }
})
```

- [ ] **步骤 5：Commit**
```bash
git add pages/projects/
git commit -m "feat: 实现项目列表页"
```

---

### 任务 5：实现我的页面（设置）

**文件：**
- 创建：`frontend/pages/mine/index.js`
- 创建：`frontend/pages/mine/index.wxml`
- 创建：`frontend/pages/mine/index.wxss`
- 创建：`frontend/pages/mine/index.json`

- [ ] **步骤 1-4：创建页面文件**

```xml
<!-- index.wxml -->
<view class="container">
  <view class="user-card">
    <text class="user-name">{{userInfo.nickname || '用户'}}</text>
  </view>
  
  <view class="menu-list">
    <view class="menu-item" bindtap="goToHabits">🔥 习惯打卡</view>
    <view class="menu-item" bindtap="goToMemory">🧠 记忆库</view>
    <view class="menu-item" bindtap="goToNotifications">🔔 通知中心</view>
    <view class="menu-item" bindtap="goToSettings">⚙️ 设置</view>
  </view>
</view>
```

- [ ] **步骤 5：Commit**
```bash
git add pages/mine/
git commit -m "feat: 实现我的页面"
```

---

### 任务 6：实现习惯打卡页面

**文件：**
- 创建：`frontend/pages/habits/index.js`
- 创建：`frontend/pages/habits/index.wxml`
- 创建：`frontend/pages/habits/index.wxss`
- 创建：`frontend/pages/habits/index.json`

- [ ] **步骤 1-4：创建页面文件**

重点：打卡按钮要足够大，连续天数要醒目

```javascript
// index.js
const app = getApp()
Page({
  data: {
    habits: [],
    stats: null
  },
  onLoad() {
    this.loadHabits()
    this.loadStats()
  },
  async loadHabits() {
    try {
      const res = await app.request('/tasks/habits/today')
      this.setData({ habits: res.habits || [], stats: res })
    } catch (e) {
      console.error('加载习惯失败', e)
    }
  },
  async logHabit(e) {
    const { id } = e.currentTarget.dataset
    try {
      await app.request(`/tasks/habits/${id}/log`, { method: 'POST', body: { status: 'completed' } })
      wx.showToast({ title: '打卡成功', icon: 'success' })
      this.loadHabits()
    } catch (e) {
      wx.showToast({ title: '打卡失败', icon: 'none' })
    }
  }
})
```

- [ ] **步骤 5：Commit**
```bash
git add pages/habits/
git commit -m "feat: 实现习惯打卡页面"
```

---

### 任务 7：实现 Inbox 页面

**文件：**
- 创建：`frontend/pages/inbox/index.js`
- 创建：`frontend/pages/inbox/index.wxml`
- 创建：`frontend/pages/inbox/index.wxss`
- 创建：`frontend/pages/inbox/index.json`

- [ ] **步骤 1-4：创建页面文件**

- [ ] **步骤 5：Commit**
```bash
git add pages/inbox/
git commit -m "feat: 实现Inbox页面"
```

---

### 任务 8：实现通知中心

**文件：**
- 创建：`frontend/pages/notifications/index.js`
- 创建：`frontend/pages/notifications/index.wxml`
- 创建：`frontend/pages/notifications/index.wxss`
- 创建：`frontend/pages/notifications/index.json`

- [ ] **步骤 1-4：创建页面文件**

- [ ] **步骤 5：Commit**
```bash
git add pages/notifications/
git commit -m "feat: 实现通知中心"
```

---

### 任务 9：创建 API封装

**文件：**
- 修改：`frontend/utils/api.js`

- [ ] **步骤 1：更新 api.js**

```javascript
const app = getApp()

module.exports = {
  // 认证
  wxLogin: (code) => app.request('/auth/wx-login', { method: 'POST', body: { code } }),
  
  // 项目
  getProjects: () => app.request('/projects'),
  createProject: (data) => app.request('/projects', { method: 'POST', body: data }),
  
  // 任务
  getTasks: (params) => app.request('/tasks' + (params ? '?' + Object.keys(params).map(k => k + '=' + params[k]).join('&') : '')),
  createTask: (data) => app.request('/tasks', { method: 'POST', body: data }),
  completeTask: (id) => app.request(`/tasks/${id}/complete`, { method: 'POST' }),
  
  // 习惯
  getTodayHabits: () => app.request('/tasks/habits/today'),
  logHabit: (taskId, data) => app.request(`/tasks/habits/${taskId}/log`, { method: 'POST', body: data }),
  getHabitStats: (taskId) => app.request(`/tasks/habits/${taskId}/stats`),
  
  // 笔记
  getNotes: (status) => app.request('/notes' + (status ? '?status=' + status : '')),
  createNote: (data) => app.request('/notes', { method: 'POST', body: data }),
  getInboxCount: () => app.request('/notes/inbox/count'),
  
  // 通知
  getNotifications: () => app.request('/notifications'),
  markNotificationRead: (id) => app.request(`/notifications/${id}/read`, { method: 'POST' })
}
```

- [ ] **步骤 2：Commit**
```bash
git add utils/api.js
git commit -m "feat: 更新API封装"
```

---

## 自检清单

- [ ] 规格覆盖度：首页、任务、项目、我的、习惯、Inbox、通知、记忆库 - 全部覆盖
- [ ] 无占位符：所有步骤都包含实际代码
- [ ] 类型一致性：API路径与后端一致
- [ ] 文件路径：所有路径都是精确的相对路径

---

## 执行方式

**推荐：子代理驱动（推荐）**

每个任务都是一个独立的子代理任务，可以并行执行关键页面（首页、任务、项目、我的可以同时开始）。

**或者：内联执行**

在当前会话中逐个完成任务。